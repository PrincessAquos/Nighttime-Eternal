extends Stat

class_name ValueStat
	
var curr_val:int

func _init(level, base_value, growth_rate).(level, base_value, growth_rate):
	curr_val = val


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
