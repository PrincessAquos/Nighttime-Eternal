///init_stats(stat, level, xp, hp, stamina, resource, attack, defense, intellect, resilience, agility, luck, vision)
var stats = argument[0];

with (stats) {
    level = argument[1];
    xp = argument[2];
    maxHP = argument[3];
    maxStamina = argument[4];
    maxResource = argument[5];
    attack = argument[6];
    defense = argument[7];
    intellect = argument[8];
    resilience = argument[9];
    agility = argument[10];
    luck = argument[11];
    vision = argument[12];
}
